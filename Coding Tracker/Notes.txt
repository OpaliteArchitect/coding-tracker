I installed these EF core NuGet packages:
Microsoft.EntityFrameworkCore -- ef core stuff, obvious
Microsoft.EntityFrameworkCore.Sqlite -- sqlite database access
Microsoft.EntityFrameworkCore.Tools -- not sure, i think migrations
Microsoft.EntityFrameworkCore.Design -- not sure, i think design time migrations??

I also installed these console UI enhancers:
Spectre.Console
Spectre.Console.Cli

I set-up `appsettings.json` configuration file, and installed these packages:
Microsoft.Extensions.Configuration -- provides ConfigurationManager
Microsoft.Extensions.Configuration.Json -- allows working with json files (.AddJsonFile extension method on configuration manager requires this)
[removed; yagni] Microsoft.Extensions.Configuration.Binder -- allows json key value pairs to bind to POCO's
[removed; yagni] Microsoft.Extensions.DependencyInjection --allows DI container/services

i setup a static configmanager class that sets up the configurationmanager to the appsettings.json

